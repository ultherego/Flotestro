package main

import "github.com/ultherego/flotestro/internal/agent"

// version is the version of the agent: the tool and the daemon come in one
// package and must not drift apart in their number.
var version = agent.Version
