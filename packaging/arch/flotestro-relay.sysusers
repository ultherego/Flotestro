# The relay service account. Separate from the agent account: an ordinary
# agent may also run on the relay machine, and its identity must not be
# readable by the relay.
u flotestro-relay - "Flotestro relay" /var/lib/flotestro-relay /usr/bin/nologin
