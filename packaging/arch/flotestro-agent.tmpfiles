# The state directory belongs to the agent and is closed to the rest of the
# system: the private key and the host identity lie in it.
d /var/lib/flotestro-agent 0700 flotestro-agent flotestro-agent -
